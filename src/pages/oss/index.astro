---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import PageHeader from '../../components/layout/PageHeader.astro';
import ProjectCard from '../../components/cards/ProjectCard.astro';

const projects = (await getCollection('projects', (e) => e.data.category === 'oss'))
  .sort(
    (a, b) =>
      (b.data.updatedDate ?? b.data.startDate).getTime() -
      (a.data.updatedDate ?? a.data.startDate).getTime(),
  );
---
<BaseLayout title="OSS" description="Open source projects.">
  <PageHeader
    title="OSS"
    description="Open-source libraries and tools, maintained as personal work."
  />
  <div class="grid">
    {projects.map((e) => <ProjectCard entry={e} />)}
  </div>
</BaseLayout>

<style>
  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 20px;
    align-items: stretch;
  }
</style>
