// Tailwind CSS v4 uses CSS-first configuration via the `@theme` directive in
// `src/styles/global.css`. This file is intentionally minimal — see
// CLAUDE.md §3 / L-S03. Most projects on v4 do not need a JS config at all.
//
// Keep this file only if a plugin or tool explicitly requires it; otherwise it
// can be deleted.
export default {};
